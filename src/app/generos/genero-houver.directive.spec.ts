import { GeneroHouverDirective } from './genero-houver.directive';

describe('GeneroHouverDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneroHouverDirective();
    expect(directive).toBeTruthy();
  });
});
