import { GeneroTraduzidoPipe } from "./generos-traduzidos.pipe";

describe('GenerosPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroTraduzidoPipe();
    expect(pipe).toBeTruthy();
  });
});
